export type IncludedList = {
  listId: string;
  listName: string;
  categoryName: string | null;
};

export type OtherCategory = {
  id: string;
  name: string;
  listName: string;
  listId: string;
};
