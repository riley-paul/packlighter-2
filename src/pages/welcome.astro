---
import RadixProvider from "@/app/components/ui/radix-provider";
import LoginCard from "@/app/modules/users/components/login-card";
import RootLayout from "@/layouts/root-layout.astro";
---

<RootLayout>
  <RadixProvider>
    <main class="w-full h-screen relative flex items-center justify-center">
      <!-- <div
        class="absolute inset-0 z-0 bg-accent-12"
        style={{
          background: "#020617",
          backgroundImage: `
      linear-gradient(to right, rgba(71,85,105,0.3) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(71,85,105,0.3) 1px, transparent 1px),
      radial-gradient(circle at 50% 50%, rgba(139,92,246,0.15) 0%, transparent 70%)
    `,
          backgroundSize: "32px 32px, 32px 32px, 100% 100%",
        }}
      >
      </div> -->
      <LoginCard />
    </main>
  </RadixProvider>
</RootLayout>
